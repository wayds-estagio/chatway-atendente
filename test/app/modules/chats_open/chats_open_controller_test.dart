import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:chatway_atendente/app/modules/chats_open/chats_open_module.dart';

void main() {
  initModule(ChatsOpenModule());
  // ChatsOpenController chatsopen;
  //
  setUp(() {
    //     chatsopen = ChatsOpenModule.to.get<ChatsOpenController>();
  });

  group('ChatsOpenController Test', () {
    //   test("First Test", () {
    //     expect(chatsopen, isInstanceOf<ChatsOpenController>());
    //   });

    //   test("Set Value", () {
    //     expect(chatsopen.value, equals(0));
    //     chatsopen.increment();
    //     expect(chatsopen.value, equals(1));
    //   });
  });
}
