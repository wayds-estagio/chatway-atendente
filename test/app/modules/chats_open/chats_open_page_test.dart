import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('ChatsOpenPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatsOpenPage(title: 'ChatsOpen')));
    //  final titleFinder = find.text('ChatsOpen');
    //  expect(titleFinder, findsOneWidget);
  });
}
