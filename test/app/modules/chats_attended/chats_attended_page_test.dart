import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('ChatsAttendedPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatsAttendedPage(title: 'ChatsAttended')));
    //  final titleFinder = find.text('ChatsAttended');
    //  expect(titleFinder, findsOneWidget);
  });
}
