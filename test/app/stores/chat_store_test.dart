import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:chatway_atendente/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ChatStore chat;
  //
  setUp(() {
    //     chat = AppModule.to.get<ChatStore>();
  });

  group('ChatStore Test', () {
    //   test("First Test", () {
    //     expect(chat, isInstanceOf<ChatStore>());
    //   });

    //   test("Set Value", () {
    //     expect(chat.value, equals(0));
    //     chat.increment();
    //     expect(chat.value, equals(1));
    //   });
  });
}
